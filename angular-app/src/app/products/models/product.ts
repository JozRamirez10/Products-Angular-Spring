export class Product{
    id!: number;
        // El signo de exclamación (!) indica que después le daremos un valor 
        // a las variables
    name!: string;
    description!: string;
    price!: number;
}